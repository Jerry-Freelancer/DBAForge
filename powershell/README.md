# DBAForge PowerShell scripts

This directory will contain the dbatools scripts used by the dedicated Worker. Every script must emit structured JSON and must not write passwords or secret values to stdout, stderr, or logs.
